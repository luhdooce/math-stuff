from pycalculator import calculate,numerize,solve
'''
Multiplication must all be done explicitly (using asterisk '*' sign). Implicit multiplication through 
the juxtaposition of two terms will not work.
'''
def main():
    #print(calculate('9^(1/(1+2))^6'))
    print(calculate('(sin(700))^2+(cos(700))^2'))
    
main()